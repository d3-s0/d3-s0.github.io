# d3-s0.github.io

if install using jekyll - Ruby must be installed.

Then install jeyll and bundler: 
```
gem install jekyll bundler
```
Then run:
```
jekyll new [project_name]
```
To test locally run:
```
bundle exec jekyll serve
```